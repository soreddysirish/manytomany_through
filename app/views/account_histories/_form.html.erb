<%= simple_form_for(@account_history) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">

    <%= f.collection_select :account_id, Account.all, :id, :name %>
    <%= f.input :amount %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
